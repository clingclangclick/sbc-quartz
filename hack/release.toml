# commit to be tagged for the new release
commit = "HEAD"

project_name = "sbc-quartz"
github_repo = "clingclangclick/sbc-quartz"
match_deps = "^github.com/(clingclangclick/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
